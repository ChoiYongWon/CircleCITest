version: 2.1
jobs:
  builld:
    docker: 
      - image: circleci/node:latest
        auth:
          username: yongwon0824 
          password: $DOCKER_PW
    steps:
      - checkout
      - run: echo "testing!!!"
  
workflows:
  version: 2.1
  build:
    jobs:
      - build
