version: 2.1

orbs:
  docker: circleci/docker@1.6.0

jobs:
  build:
    docker: 
      - image: circleci/node:latest
        
    steps:
      - checkout
      - docker/check
      - run: echo "SUCCESS"
  
workflows:
  version: 2.1
  build:
    jobs:
      - build
