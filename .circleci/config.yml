version: 2
jobs:
  push:
    executor : docker/docker
    steps:
      - run: |
        docker login
      - run:
        command: |
          echo "Digest is: $(</tmp/digest.txt)"
          echo "testing!!!"
  
workflows:
  version: 2
  one_and_two:
    jobs:
      - push
      - two:
          requires:
            - push